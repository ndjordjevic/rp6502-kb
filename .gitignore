# Local-only editor / assistant state
.claude/settings.local.json
.claude/.openrouter-settings.json
.obsidian/workspace.json

# Generated analysis artifacts (not source-controlled)
all_pages.txt
linked_pages.txt

# Raw GitHub clones — plain clones, not submodules; untracked by parent repo
raw/github/*
!raw/github/README.md

# Local PDFs (datasheets, books) — not source-controlled
raw/pdfs/**/*.pdf
